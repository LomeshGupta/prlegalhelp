import React, { useRef } from "react";
import "./home.css";
import Slideshow from "./slideshow";

const Home = () => {
  return (
    <section class="homesection" id="home">
      <Slideshow />
    </section>
  );
};

export default Home;
